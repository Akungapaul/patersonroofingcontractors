---
phase: 03-service-guide-utility-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/sections/ServiceOverview.tsx
  - components/sections/ProcessSteps.tsx
  - components/sections/BenefitsGrid.tsx
  - components/sections/RelatedLocations.tsx
  - components/sections/RelatedServices.tsx
  - components/sections/ServiceFAQ.tsx
autonomous: true
requirements: [SVC-03, SVC-04, SVC-05]

must_haves:
  truths:
    - "ServiceOverview renders overviewHtml via dangerouslySetInnerHTML with correct prose styling"
    - "ProcessSteps renders 4-6 numbered steps with decorative step numbers and optional icons"
    - "BenefitsGrid renders 4-6 benefit cards with amber top border and icons"
    - "RelatedLocations renders all 16 municipality cards from siteConfig with links to location pages"
    - "RelatedServices renders 3-4 service cards with links to /services/{slug}"
    - "ServiceFAQ is a client component with accordion behavior matching CityFAQ pattern"
  artifacts:
    - path: "components/sections/ServiceOverview.tsx"
      provides: "Service description section"
      contains: "dangerouslySetInnerHTML"
    - path: "components/sections/ProcessSteps.tsx"
      provides: "Process steps section"
      contains: "ProcessStep"
    - path: "components/sections/BenefitsGrid.tsx"
      provides: "Benefits grid section"
      contains: "Benefit"
    - path: "components/sections/RelatedLocations.tsx"
      provides: "16 municipality links grid"
      contains: "siteConfig.municipalities"
    - path: "components/sections/RelatedServices.tsx"
      provides: "Related services cards"
      contains: "/services/"
    - path: "components/sections/ServiceFAQ.tsx"
      provides: "FAQ accordion for service pages"
      contains: "'use client'"
  key_links:
    - from: "components/sections/RelatedLocations.tsx"
      to: "lib/site-config.ts"
      via: "siteConfig.municipalities import"
      pattern: "import.*siteConfig.*from.*site-config"
    - from: "components/sections/ServiceFAQ.tsx"
      to: "data/content/types.ts"
      via: "FaqItem type import"
      pattern: "import.*FaqItem"
---

<objective>
Create all 6 section components used on service pages: ServiceOverview, ProcessSteps, BenefitsGrid, RelatedLocations, RelatedServices, and ServiceFAQ.

Purpose: These components are the visual building blocks for all 67 service pages. They follow the section-based composition pattern from Phase 1/2 and the visual contracts from UI-SPEC.

Output: 6 new component files in components/sections/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-service-guide-utility-pages/03-CONTEXT.md
@.planning/phases/03-service-guide-utility-pages/03-UI-SPEC.md

<interfaces>
<!-- Key types and contracts. -->

From data/content/types.ts:
```typescript
export interface FaqItem {
  readonly question: string
  readonly answer: string
}
```

From data/services/types.ts (created in Plan 01):
```typescript
export interface ProcessStep {
  readonly title: string
  readonly description: string
  readonly icon?: string
}

export interface Benefit {
  readonly title: string
  readonly description: string
  readonly icon: string
}
```

From lib/site-config.ts (municipality structure):
```typescript
siteConfig.municipalities: { name: string; slug: string; population: number; cluster: string }[]
```

From components/ui/Card.tsx:
```typescript
export function Card({ children, interactive, className }: CardProps)
```

From components/sections/CityFAQ.tsx (pattern to clone for ServiceFAQ):
```typescript
'use client'
import { useState } from 'react'
import { ChevronDown } from 'lucide-react'
import { cn } from '@/lib/cn'
import type { FaqItem } from '@/data/content/types'

interface CityFAQProps {
  readonly faqItems: readonly FaqItem[]
  readonly cityName: string
}
export function CityFAQ({ faqItems, cityName }: CityFAQProps)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ServiceOverview, ProcessSteps, and BenefitsGrid components</name>
  <files>components/sections/ServiceOverview.tsx, components/sections/ProcessSteps.tsx, components/sections/BenefitsGrid.tsx</files>
  <read_first>
    - components/sections/CityIntro.tsx (pattern for dangerouslySetInnerHTML rendering, prose styling)
    - components/ui/Card.tsx (Card component API)
    - .planning/phases/03-service-guide-utility-pages/03-UI-SPEC.md (ServiceOverview, ProcessSteps, BenefitsGrid visual contracts)
    - lib/cn.ts (cn utility for conditional classes)
  </read_first>
  <action>
**components/sections/ServiceOverview.tsx** (Server component):

Props: `{ overviewHtml: string; serviceName: string }`

Structure per UI-SPEC:
- Section: `bg-white py-section-sm lg:py-section`
- Container: `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8`
- H2: `{serviceName}` with classes `text-[28px] md:text-3xl font-bold text-navy font-heading`
- Prose container: `max-w-4xl mx-auto` wrapping a div with `dangerouslySetInnerHTML={{ __html: overviewHtml }}`
- Prose classes: `text-lg text-gray-700 leading-relaxed [&>p]:mb-4 [&>a]:text-amber-dark [&>a]:hover:text-amber [&>a]:underline [&>p>a]:text-amber-dark [&>p>a]:hover:text-amber [&>p>a]:underline`

**components/sections/ProcessSteps.tsx** (Server component):

Props: `{ steps: readonly ProcessStep[]; serviceName: string }`

Import `ProcessStep` from `@/data/services/types`. Dynamically import Lucide icons using a helper or pass through without rendering if icon not in map. For simplicity, import commonly used icons and build a lookup map (similar to ServicesGrid iconMap pattern).

Structure per UI-SPEC:
- Section: `bg-gray-light py-section-sm lg:py-section` (use `bg-[#F8FAFC]`)
- Container: `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8`
- H2: `"Our {serviceName} Process"` centered, `text-[28px] md:text-3xl font-bold text-navy font-heading text-center`
- Grid: `grid grid-cols-1 md:grid-cols-2 gap-8 mt-8`
- Each step card: white bg, `p-6 rounded-lg shadow-sm bg-white`
  - Step number: `text-5xl font-bold text-amber/20 font-heading` (decorative, large, faded)
  - Step icon (optional): if icon provided and in map, render `h-6 w-6 text-amber` next to title
  - Step title: `text-2xl font-bold text-navy font-heading mt-2` (H3 element)
  - Step description: `text-lg text-gray-700 mt-2 leading-relaxed`

**components/sections/BenefitsGrid.tsx** (Server component):

Props: `{ benefits: readonly Benefit[]; serviceName: string }`

Import `Benefit` from `@/data/services/types`. Use same icon lookup map pattern as ProcessSteps.

Structure per UI-SPEC:
- Section: `bg-white py-section-sm lg:py-section`
- Container: `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8`
- H2: `"Benefits of {serviceName}"` centered, `text-[28px] md:text-3xl font-bold text-navy font-heading text-center`
- Grid: `grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3 mt-8`
- Each benefit card: `border-t-[3px] border-t-amber p-6 rounded-lg shadow-sm bg-white`
  - Icon: `h-10 w-10 text-amber mb-4`
  - Title: `text-2xl font-bold text-navy font-heading` (H3 element)
  - Description: `text-lg text-gray-700 mt-2 leading-relaxed`
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/patersonroofingcontractors && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - components/sections/ServiceOverview.tsx contains `dangerouslySetInnerHTML`
    - components/sections/ServiceOverview.tsx contains `export function ServiceOverview`
    - components/sections/ServiceOverview.tsx contains `max-w-4xl`
    - components/sections/ProcessSteps.tsx contains `export function ProcessSteps`
    - components/sections/ProcessSteps.tsx contains `text-5xl font-bold text-amber/20`
    - components/sections/ProcessSteps.tsx contains `import type { ProcessStep }`
    - components/sections/BenefitsGrid.tsx contains `export function BenefitsGrid`
    - components/sections/BenefitsGrid.tsx contains `border-t-[3px] border-t-amber`
    - components/sections/BenefitsGrid.tsx contains `import type { Benefit }`
    - TypeScript compiles with zero errors
  </acceptance_criteria>
  <done>ServiceOverview renders HTML content with prose styling. ProcessSteps renders numbered step cards in 2-column grid with decorative numbers. BenefitsGrid renders benefit cards with amber top border in 3-column grid. All compile cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Create RelatedLocations, RelatedServices, and ServiceFAQ components</name>
  <files>components/sections/RelatedLocations.tsx, components/sections/RelatedServices.tsx, components/sections/ServiceFAQ.tsx</files>
  <read_first>
    - lib/site-config.ts (siteConfig.municipalities structure for RelatedLocations)
    - components/ui/Card.tsx (Card component props)
    - components/sections/CityFAQ.tsx (full component code -- clone accordion pattern exactly for ServiceFAQ)
    - .planning/phases/03-service-guide-utility-pages/03-UI-SPEC.md (RelatedLocations, RelatedServices, ServiceFAQ visual contracts)
  </read_first>
  <action>
**components/sections/RelatedLocations.tsx** (Server component):

Props: `{ serviceName: string }`

Import `siteConfig` from `@/lib/site-config`, `Card` from `@/components/ui/Card`, `Link` from `next/link`.

Structure per UI-SPEC:
- Section: `bg-[#F8FAFC] py-section-sm lg:py-section`
- Container: `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8`
- H2: `"{serviceName} Across Passaic County"` centered, `text-[28px] md:text-3xl font-bold text-navy font-heading text-center`
- Grid: `grid grid-cols-2 gap-4 sm:grid-cols-3 md:grid-cols-4 mt-8`
- Each card: `Link` wrapping `Card interactive` with `p-4 text-center`
  - City name: `text-lg font-bold text-navy`
  - Link text: `text-sm text-amber hover:text-amber-dark` -- "View {city} services"
  - href: `/roofing-contractor-${municipality.slug}-nj`
- Data source: `siteConfig.municipalities` (all 16)

**components/sections/RelatedServices.tsx** (Server component):

Props: `{ services: readonly { name: string; slug: string; description: string }[] }`

Import `Card` from `@/components/ui/Card`, `Link` from `next/link`, `ArrowRight` from `lucide-react`.

Structure per UI-SPEC:
- Section: `bg-white py-section-sm lg:py-section`
- Container: `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8`
- H2: `"Related Services"` centered, `text-[28px] md:text-3xl font-bold text-navy font-heading text-center`
- Grid: `grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-4 mt-8`
- Each card: `Link` wrapping `Card interactive` with `p-6`
  - Service name: `text-2xl font-bold text-navy font-heading`
  - Description: `text-lg text-gray-700 mt-2 line-clamp-3`
  - Arrow: `ArrowRight` icon, `h-5 w-5 text-amber mt-4`
  - href: `/services/${service.slug}`

**components/sections/ServiceFAQ.tsx** (Client component -- `'use client'`):

Props: `{ faqItems: readonly FaqItem[]; serviceName: string }`

Clone the CityFAQ component exactly, changing only:
1. Component name: `ServiceFAQ` instead of `CityFAQ`
2. H2 heading text: `"Frequently Asked Questions About {serviceName}"` instead of city-based heading
3. Props interface: `ServiceFAQProps` with `serviceName` instead of `cityName`

Keep identical accordion behavior: `useState<number | null>(null)` for openIndex, toggle function, `max-h-0`/`max-h-[1000px]` transition, `ChevronDown` rotation, `aria-expanded`, `aria-controls`, `hover:bg-gray-50`, `motion-reduce:transition-none`.

Section wrapper: `bg-[#F8FAFC] py-section-sm lg:py-section`
Container: `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8`
FAQ container: `max-w-3xl mx-auto`
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/patersonroofingcontractors && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - components/sections/RelatedLocations.tsx contains `export function RelatedLocations`
    - components/sections/RelatedLocations.tsx contains `siteConfig.municipalities`
    - components/sections/RelatedLocations.tsx contains `roofing-contractor-`
    - components/sections/RelatedServices.tsx contains `export function RelatedServices`
    - components/sections/RelatedServices.tsx contains `/services/`
    - components/sections/RelatedServices.tsx contains `ArrowRight`
    - components/sections/ServiceFAQ.tsx contains `'use client'`
    - components/sections/ServiceFAQ.tsx contains `export function ServiceFAQ`
    - components/sections/ServiceFAQ.tsx contains `aria-expanded`
    - components/sections/ServiceFAQ.tsx contains `Frequently Asked Questions About`
    - TypeScript compiles with zero errors
  </acceptance_criteria>
  <done>RelatedLocations renders all 16 municipality cards. RelatedServices renders service cards with arrow indicators. ServiceFAQ renders accessible FAQ accordion matching CityFAQ pattern. All compile cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| overviewHtml rendering | Developer-authored HTML rendered via dangerouslySetInnerHTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | T (Tampering) | ServiceOverview dangerouslySetInnerHTML | accept | Content is developer-authored in TypeScript data files, not user input. Same pattern as CityIntro.tsx which renders introHtml. No user-generated content enters this path. |
| T-03-04 | S (Spoofing) | RelatedLocations links | accept | Links point to internal location pages using siteConfig municipality slugs. No user-controlled URL segments. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes with zero errors
- All 6 component files exist in components/sections/
- ServiceFAQ has `'use client'` directive
- Other 5 components are server components (no `'use client'`)
</verification>

<success_criteria>
- ServiceOverview renders HTML prose with dangerouslySetInnerHTML
- ProcessSteps renders numbered cards with decorative step numbers in 2-col grid
- BenefitsGrid renders benefit cards with amber top border and icons in 3-col grid
- RelatedLocations renders all 16 municipality cards from siteConfig
- RelatedServices renders service cards with ArrowRight icons
- ServiceFAQ has full accordion behavior matching CityFAQ
- All 6 compile cleanly with TypeScript
</success_criteria>

<output>
After completion, create `.planning/phases/03-service-guide-utility-pages/03-02-SUMMARY.md`
</output>
