---
phase: 2
slug: anchor-city-location-pages
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-10
---

# Phase 2 -- UI Design Contract

> Visual and interaction contract for Phase 2: Anchor City & Location Pages. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none |
| Preset | not applicable |
| Component library | none (custom components, no shadcn -- inherits Phase 1) |
| Icon library | lucide-react 1.7.0 (already installed) |
| Font (body) | Cormorant Garamond, weight 500 (medium), via next/font/google |
| Font (headings) | Cormorant, weights 500 + 700, via next/font/google |

**Source:** Phase 1 UI-SPEC (design system carries forward unchanged). No new dependencies for Phase 2.

---

## Spacing Scale

Inherited from Phase 1 without modification:

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing, neighborhood card internal padding top/bottom |
| md | 16px | Default element spacing, card padding, FAQ item padding |
| lg | 24px | Section inner padding, card content spacing |
| xl | 32px | Layout gaps between grid items, gap between neighborhood cards |
| 2xl | 48px | Section vertical padding on mobile (`py-section-sm`) |
| 3xl | 80px | Section vertical padding on desktop (`py-section`) |

Exceptions:
- Touch targets: 44px minimum height for all tappable elements (FAQ toggle buttons, CTA buttons, phone links).
- Hero section: min-h-[70vh] on location pages (shorter than homepage 100vh -- location pages are content-heavy, hero is gateway not destination).

**Source:** Phase 1 UI-SPEC spacing scale. Hero height exception is Phase 2 specific -- location pages have 3000+ words of content below the fold, so the hero should be a prominent introduction without consuming the full viewport.

---

## Typography

4 font sizes, 2 font weights. Inherited scale from Phase 1, consolidated for Phase 2.

| Role | Size | Weight | Line Height | Font Family |
|------|------|--------|-------------|-------------|
| Body | 18px (text-lg) | 500 (medium) | 1.6 | Cormorant Garamond |
| Subheading (H3) | 24px (text-2xl) | 700 (bold) | 1.3 | Cormorant |
| Heading (H2) | 32px (text-3xl) | 700 (bold) | 1.2 | Cormorant |
| Display (H1) | 48px (text-5xl) | 700 (bold) | 1.1 | Cormorant |

**Responsive breakpoint adaptations (same 4-size scale, not additional sizes):**
- H1: 48px desktop, 36px mobile (`text-4xl md:text-5xl`). The 36px is a responsive adaptation of the 48px display size.
- H2: 32px desktop, 28px mobile (`text-[28px] md:text-3xl`). The 28px is a responsive adaptation of the 32px heading size.

**Phase 2 typography notes:**
- Location page H1: `"Expert Roofing Contractors in {City}, NJ"` -- 48px desktop, 36px mobile.
- Section H2s: `"About Roofing in {City}"`, `"Neighborhoods We Serve in {City}"`, `"Frequently Asked Questions"` -- 32px desktop, 28px mobile. Consistent with Phase 1 pattern.
- FAQ question text: 24px (`text-2xl`) weight 700 (bold), Cormorant Garamond. Uses the H3/subheading size to create clear visual distinction from 18px answer text without introducing an additional scale entry.
- FAQ answer text: 18px body standard, weight 500, line-height 1.6.
- Neighborhood card name: 24px (`text-2xl`) weight 700 (bold), Cormorant (heading font). Uses the H3/subheading size for consistent card title treatment.
- Neighborhood card roofingContext: 18px (`text-lg`) weight 500, line-height 1.6, `text-gray-600`. Uses body size for consistency; card layout and `text-gray-600` color provide sufficient visual hierarchy without a smaller size.
- CityIntro body prose (introHtml): 18px body standard, rendered as HTML with `prose`-style paragraph spacing (mb-4 between paragraphs).

**Source:** Phase 1 UI-SPEC typography scale, consolidated from 6+ sizes to 4 sizes (18, 24, 32, 48) with 2 responsive adaptations (28, 36). Weights reduced from 3 (500, 600, 700) to 2 (500, 700).

---

## Color

Inherited from Phase 1 without modification:

| Role | Value | Tailwind Token | Usage |
|------|-------|----------------|-------|
| Dominant (60%) | #FFFFFF / #F8FAFC | white / gray-light | Page backgrounds, alternating section backgrounds |
| Secondary (30%) | #1B2A4A | navy | Hero overlay, mid-page CTA bars, header, footer |
| Accent (10%) | #D97706 | amber | See reserved-for list below |
| Navy Light | #2A3F6A | navy-light | Hero gradient endpoint |
| Navy Dark | #111D35 | navy-dark | Hero gradient start |
| Amber Light | #F59E0B | amber-light | Star ratings, phone link highlights |
| Amber Dark | #B45309 | amber-dark | Primary CTA hover state |
| Destructive | #DC2626 | red-600 | Form validation errors only |
| Success | #16A34A | green-600 | Form submission success state only |

**Accent (#D97706 amber) reserved for (Phase 2 additions in bold):**
1. Primary CTA buttons ("Get Free Estimate", "Get Quote", "Call Now")
2. Phone CTA button in sticky header
3. Emergency storm damage banner background
4. Card hover border accent (service cards, **neighborhood cards**)
5. Star ratings in testimonials
6. Hover fill on secondary (outline) buttons
7. **Card top border accent on neighborhood cards** (3px `border-t-amber`)
8. **FAQ toggle icon color** (chevron/plus icon)
9. **CityIntro section decorative accent** (amber left border on blockquote-style local context callout)

**Section background alternation pattern for location pages:**
1. Hero -- navy gradient overlay on cluster image
2. CityIntro (About Roofing in [City]) -- white (#FFFFFF)
3. MidPageCTA -- navy (#1B2A4A)
4. ServicesGrid -- gray-light (#F8FAFC)
5. NeighborhoodGrid -- white (#FFFFFF)
6. MidPageCTA -- navy (#1B2A4A)
7. WhyChooseUs -- gray-light (#F8FAFC)
8. CityFAQ -- white (#FFFFFF)
9. Testimonials -- gray-light (#F8FAFC)
10. EmergencyCTA -- amber (#D97706)
11. ContactForm -- gray-light (#F8FAFC)

**Source:** Phase 1 UI-SPEC color contract. Section alternation follows the white/gray-light rhythm established in Phase 1, adapted to the D-01 section flow.

---

## Component Inventory

### New Components (Phase 2)

| Component | File | Server/Client | Key Props |
|-----------|------|---------------|-----------|
| CityIntro | components/sections/CityIntro.tsx | Server | `introHtml: string`, `localContext: string`, `cityName: string` |
| NeighborhoodGrid | components/sections/NeighborhoodGrid.tsx | Server | `neighborhoods: Neighborhood[]`, `cityName: string` |
| CityFAQ | components/sections/CityFAQ.tsx | Client (`'use client'`) | `faqItems: FaqItem[]`, `cityName: string` |

### Reused Components (from Phase 1, no modifications)

| Component | Usage on Location Pages |
|-----------|------------------------|
| Hero | Reuse with location-specific props: H1 text, subheadline, cluster-specific hero image |
| ServicesGrid | Reuse directly -- displays relevant services with silo links |
| WhyChooseUs | Reuse directly -- same trust messaging and stats |
| Testimonials | Reuse directly -- same placeholder reviews |
| EmergencyCTA | Reuse directly -- same storm damage banner |
| ContactForm | Reuse directly -- same form at bottom of every location page |
| Card | Reuse for neighborhood cards |
| Button | Reuse for all CTAs |
| PhoneButton | Reuse in hero and CTAs |
| JsonLd | Reuse for LocalBusiness + FAQ + BreadcrumbList schema rendering |
| Breadcrumbs | Reuse with location-specific breadcrumb trail |

### Modified Components (Phase 1 components needing prop extensions)

| Component | Modification | Reason |
|-----------|-------------|--------|
| MidPageCTA | Add optional `cityName?: string` prop | City-specific messaging: "Need a roof estimate in {City}?" (D-04) |
| Hero | Add props: `headline: string`, `subheadline: string`, `backgroundImage?: string` | Location pages pass city-specific hero content instead of homepage defaults |
| ServicesGrid | Add optional `serviceSlugs?: string[]` prop | Location pages show only relevant services, not all (LOC-06) |

---

## New Component Visual Contracts

### CityIntro

**Purpose:** Renders the "About Roofing in {City}" section with 800-1500 words of unique locally-relevant content.

| Property | Value |
|----------|-------|
| Background | White (#FFFFFF) |
| Container | `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8` |
| Section padding | `py-section-sm lg:py-section` (48px mobile, 80px desktop) |
| H2 heading | "About Roofing in {City}" -- `text-[28px] md:text-3xl font-bold text-navy font-heading` |
| Body prose | `introHtml` rendered via `dangerouslySetInnerHTML` -- 18px body, `text-gray-700`, paragraph spacing `mb-4` (via Tailwind prose-like classes on wrapper div) |
| Local context callout | `localContext` rendered as a styled blockquote: 4px amber left border (`border-l-4 border-l-amber`), `pl-6`, `bg-gray-light/50` padding `py-4`, italic text (`italic text-gray-700`), 18px. Placed after the first 2-3 paragraphs of introHtml or at the end of the intro section. |
| Prose link styling | Links within introHtml: `text-amber-dark hover:text-amber underline` -- points to service pages (silo linking within content) |
| Max content width | `max-w-4xl` on the prose container for readable line length (~70-80 characters) |

### NeighborhoodGrid

**Purpose:** Displays 6-12 neighborhood cards showing neighborhood name and roofing context.

| Property | Value |
|----------|-------|
| Background | White (#FFFFFF) |
| Container | `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8` |
| Section padding | `py-section-sm lg:py-section` |
| H2 heading | "Neighborhoods We Serve in {City}" -- `text-[28px] md:text-3xl font-bold text-navy font-heading` |
| Grid layout | `grid grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-3` (D-03: 2-3 column grid) |
| Card component | Reuses `<Card>` without `interactive` prop (informational only) |
| Card styling | `border-t-[3px] border-t-amber p-6` (matches ServicesGrid card pattern) |
| Neighborhood name | `text-2xl font-bold text-navy font-heading` (Cormorant, 24px, bold) |
| Roofing context | `mt-2 text-lg text-gray-600` (18px, weight 500, 1-2 sentences) |
| Card count | Paterson: 10-12 cards. Other cities: 6-8 cards. (D-03, D-10) |
| Empty guard | Component does not render if neighborhoods array is empty (defensive, should never happen with valid content) |

### CityFAQ

**Purpose:** FAQ accordion section with location-specific Q&A. Client component for toggle interactivity.

| Property | Value |
|----------|-------|
| Background | White (#FFFFFF) |
| Container | `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8` |
| Section padding | `py-section-sm lg:py-section` |
| H2 heading | "Frequently Asked Questions About Roofing in {City}" -- `text-[28px] md:text-3xl font-bold text-navy font-heading` |
| FAQ container | `max-w-3xl mx-auto` -- centered, narrower than full width for readability |
| FAQ item wrapper | `border-b border-gray-200` -- bottom border separator between items. First item: `border-t border-gray-200` (top border on first item only). |
| Question button | `w-full flex items-center justify-between py-5 text-left` -- full-width clickable area for WCAG compliance |
| Question text | `text-2xl font-bold text-navy` (24px, Cormorant Garamond, weight 700) |
| Toggle icon | Lucide `ChevronDown` icon, `h-5 w-5 text-amber transition-transform duration-200`. Rotates 180deg when open: `rotate-180`. |
| Answer panel | Hidden by default. When open: `pb-5` padding below, smooth height transition via `overflow-hidden` + CSS `grid-rows` animation or `max-height` transition. |
| Answer text | `text-lg text-gray-700 leading-relaxed` (18px body standard). Supports HTML content via `dangerouslySetInnerHTML` for rich formatting. |
| Default state | All FAQ items collapsed on page load. |
| Interaction | Click/tap question to toggle open/closed. Only one item open at a time (accordion pattern -- opening one closes the previously open item). |
| Keyboard | `Enter` or `Space` toggles FAQ item. `aria-expanded` on question button. `aria-controls` pointing to answer panel `id`. Answer panel has `role="region"` and `aria-labelledby` pointing to question button `id`. |
| Item count | Paterson: 8 items. Other cities: 5-6 items. (D-07, D-10) |

---

## Location Page Section Order

The exact visual ordering from top to bottom for all 16 location pages, as locked in CONTEXT.md D-01:

1. **Breadcrumbs** -- `Home > {City} Roofing` visual trail + BreadcrumbList JSON-LD
2. **Hero** -- Cluster-specific background image with navy overlay, city-specific H1 + subheadline + phone CTA + "Get Free Estimate" button. `min-h-[70vh]`.
3. **CityIntro** (About Roofing in [City]) -- White bg. 800-1500 words unique prose content with local context callout.
4. **MidPageCTA** -- Navy bg. "Need a roof estimate in {City}? Call (973) 555-0100 or [Get Quote]" (D-04: placed after About section)
5. **ServicesGrid** -- Gray-light bg. Relevant services only (filtered by `relevantServiceSlugs`). Silo links to service pages.
6. **NeighborhoodGrid** -- White bg. 6-12 neighborhood cards with roofing context.
7. **MidPageCTA** -- Navy bg. Repeated. (D-04: placed after Neighborhoods section)
8. **WhyChooseUs** -- Gray-light bg. Trust badges + social proof stats (reused directly).
9. **CityFAQ** -- White bg. 5-8 FAQ items with accordion interaction + FAQ JSON-LD schema.
10. **Testimonials** -- Gray-light bg. 3 review cards (reused directly).
11. **EmergencyCTA** -- Amber bg. Storm damage banner (reused directly).
12. **ContactForm** -- Gray-light bg. Lead capture form (reused directly).

**JSON-LD schemas rendered at top of page (invisible):**
- LocalBusiness (with city-specific `addressLocality`)
- FAQ (from `faqItems` array)
- BreadcrumbList (Home > {City} Roofing)

**Source:** CONTEXT.md D-01 (section flow), D-04 (mid-page CTA placement).

---

## Hero Variations by Geographic Cluster

| Cluster | Cities | Hero Image Description | Overlay |
|---------|--------|----------------------|---------|
| Urban | Paterson, Clifton, Passaic, Haledon, Prospect Park | Dense housing / city skyline view. Multi-story buildings, flat roofs, urban streetscape. | `bg-navy/70` (70% opacity navy overlay) |
| Suburban | Wayne, Hawthorne, Little Falls, Woodland Park, Totowa, North Haledon | Suburban neighborhood with single-family homes, tree-lined streets, peaked roofs. | `bg-navy/70` |
| Highlands | West Milford, Ringwood, Wanaque, Pompton Lakes, Bloomingdale | Wooded/rural homes surrounded by trees, hilly terrain, residential rooftops. | `bg-navy/70` |

**Image sourcing (Claude's discretion per CONTEXT.md):** Use high-quality royalty-free stock photos from Unsplash for initial implementation. 3 images total (one per cluster). If images are not sourced in time, use a solid navy gradient fallback (`bg-gradient-to-br from-navy-dark via-navy to-navy-light`) matching the homepage hero pattern.

**Image implementation:** Next.js `<Image>` component with `fill` prop, `priority` loading (hero is LCP element), `object-cover` fit, and descriptive alt text: `"Roofing services in {City}, NJ -- {cluster} area homes and buildings"`.

**Source:** CONTEXT.md D-02 (hero images per cluster), RESEARCH.md Open Question 2 (image sourcing).

---

## Interaction Contracts

### FAQ Accordion

| State | Visual |
|-------|--------|
| All collapsed (default) | Question text visible, chevron pointing down, answer hidden |
| Item opening | Chevron rotates 180deg (duration-200), answer panel expands with smooth height transition (duration-300) |
| Item open | Chevron pointing up (amber color), answer text visible with `pb-5` |
| Item closing | Reverse of opening -- chevron rotates back, answer collapses |
| Hover on question | Question text: no color change. Background: `hover:bg-gray-50` subtle highlight on the question row |
| Focus-visible | 2px amber outline, 2px offset on question button (consistent with Phase 1 focus pattern) |
| Reduced motion | `motion-reduce:transition-none` on chevron rotation and answer panel height |

**Keyboard:** Tab navigates between question buttons. Enter/Space toggles. No arrow key navigation (each question is a standalone button, not a roving tabindex group).

### Neighborhood Card Hover

Neighborhood cards are purely informational (no click action). Do NOT use the `interactive` prop on `<Card>`. Use `<Card className="border-t-[3px] border-t-amber p-6">` without hover effects. This avoids misleading users into thinking the cards are clickable. Use `tabIndex={-1}` to keep them out of tab order since they are non-interactive content.

### CityIntro Local Context Callout

| State | Visual |
|-------|--------|
| Default | Left-aligned blockquote with 4px amber left border, light gray background tint, italic text |
| No interaction | Static content, no hover/focus states |

### MidPageCTA City Personalization

| State | Visual |
|-------|--------|
| With cityName prop | "Need a roof estimate in {City}? Call (973) 555-0100 or [Get Quote]" |
| Without cityName (fallback) | "Need a roofing estimate? Call (973) 555-0100 or [Get Quote]" (existing behavior) |

Phone number link in mid-page CTA: `text-amber-light underline hover:text-amber` (already established in Phase 1).

### Contact Form (location pages)

No changes from Phase 1. Same form states (idle, submitting, success, error). Rendered at bottom of every location page.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Hero H1 (template) | Expert Roofing Contractors in {City}, NJ |
| Hero subheadline (template) | Trusted roof repair, replacement & installation for {City} homes and businesses. Serving {cluster area description} for 15+ years. |
| Primary CTA (hero) | Get Free Estimate |
| Secondary CTA (mid-page, with city) | Need a roof estimate in {City}? Call (973) 555-0100 or [Get Quote] |
| CityIntro H2 | About Roofing in {City} |
| NeighborhoodGrid H2 | Neighborhoods We Serve in {City} |
| CityFAQ H2 | Frequently Asked Questions About Roofing in {City} |
| Breadcrumb trail | Home > {City} Roofing |
| SEO title (template) | Roofing Contractor {City} NJ \| Roof Repair & Replacement |
| SEO description (template) | Top-rated roofing contractor in {City}, NJ. Expert roof repair, replacement, and installation for residential and commercial properties. Free estimates. Call (973) 555-0100. |

### City-Specific Copy (populated per CityContent data file)

| Element | Source | Example (Paterson) |
|---------|--------|--------------------|
| heroHeadline | `CityContent.heroHeadline` | Expert Roofing Contractors in Paterson, NJ |
| heroSubheadline | `CityContent.heroSubheadline` | Trusted roof repair, replacement & installation for Paterson homes and businesses. Serving the Silk City for 15+ years. |
| introHtml | `CityContent.introHtml` | 800-1500 words of unique prose (1200-1500 for Paterson anchor) |
| localContext | `CityContent.localContext` | Geographic angle paragraph rendered as callout |
| FAQ questions | `CityContent.faqItems[].question` | "How much does a roof replacement cost in Paterson, NJ?" |
| FAQ answers | `CityContent.faqItems[].answer` | Detailed answer with local pricing/context |
| seoTitle | `CityContent.seoTitle` | Roofing Contractor Paterson NJ \| Roof Repair & Replacement |
| seoDescription | `CityContent.seoDescription` | Top-rated roofing contractor in Paterson, NJ... |

### Empty State

Not applicable for Phase 2. All location pages are statically generated at build time with content from TypeScript data files. There is no user-generated or data-dependent content that could be empty. If a `CityContent` lookup fails (invalid slug), the page returns `notFound()` (Next.js 404 page), not an empty state.

### Error State

| Scenario | Behavior |
|----------|----------|
| Invalid location slug | `notFound()` -- renders Next.js 404 page. Not a location page concern (handled by `dynamicParams = false`). |
| Contact form errors | Same error states as Phase 1: "Something went wrong. Please call us directly at (973) 555-0100." |
| Missing hero image | Fallback to navy gradient: `bg-gradient-to-br from-navy-dark via-navy to-navy-light` (same as current homepage hero). |

### Destructive Actions

None in Phase 2. Location pages are read-only content pages. The contact form has no delete/clear functionality that would need confirmation.

**Source:** CONTEXT.md D-01 (section headings), D-04 (mid-page CTA copy), D-10 (Paterson-specific content), RESEARCH.md Pattern 1 (SEO metadata pattern).

---

## Responsive Breakpoints

Inherited from Phase 1. Phase 2 specific adaptations:

| Breakpoint | Width | Location Page Layout Changes |
|------------|-------|------------------------------|
| Default (mobile) | < 640px | Single column everything. FAQ full-width. Neighborhoods 1-col. Hero H1 36px. |
| sm | >= 640px | Neighborhoods 1-col still. Slightly larger spacing. |
| md | >= 768px | Neighborhoods 2-col grid. Services 2-col. Hero H1 48px. |
| lg | >= 1024px | Neighborhoods 3-col grid. Services 3-col. CityIntro prose max-w-4xl centered. |
| xl | >= 1280px | Max-width container (1280px centered). Full desktop layout. |

**Container:** All section components use `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8` (consistent with Phase 1).

**CityIntro prose container:** `max-w-4xl` nested within the `max-w-7xl` container for comfortable reading width.

**CityFAQ container:** `max-w-3xl mx-auto` for focused reading of Q&A content.

---

## Accessibility Contract

Inherited from Phase 1 plus Phase 2 additions:

| Requirement | Implementation |
|-------------|----------------|
| FAQ keyboard access | Question buttons are native `<button>` elements. `aria-expanded="true/false"`. `aria-controls="{answerId}"`. Answer panels have `role="region"` and `aria-labelledby="{questionId}"`. |
| FAQ screen reader | Each question button announces expanded/collapsed state. Answer content is hidden from screen readers when collapsed (`aria-hidden="true"` or removed from DOM). |
| Heading hierarchy | H1: city hero heading. H2: section headings (CityIntro, ServicesGrid, NeighborhoodGrid, WhyChooseUs, CityFAQ, Testimonials). H3: individual neighborhood names within NeighborhoodGrid (use `<h3>` to maintain strict hierarchy under the H2 "Neighborhoods We Serve"). |
| CityIntro HTML safety | `introHtml` rendered via `dangerouslySetInnerHTML`. Content is authored at build time by developers (not user-submitted), so XSS risk is mitigated. Do NOT render user-submitted HTML this way. |
| Image alt text | Hero images: `"Roofing services in {City}, NJ -- {cluster description} area homes and buildings"`. Decorative icons: `aria-hidden="true"`. |
| Breadcrumb navigation | `<nav aria-label="Breadcrumb">` wrapping an `<ol>` with `<li>` items. Current page item has `aria-current="page"`. |
| Focus visibility | All interactive elements: 2px amber outline, 2px offset, `:focus-visible` only. Applies to FAQ buttons, CTA buttons, phone links. |
| Reduced motion | FAQ chevron rotation and answer panel transitions respect `motion-reduce:transition-none`. |
| Touch targets | FAQ question buttons: full-width, minimum 44px tall (ensured by `py-5` = 40px padding + text height). CTA buttons: min-h-[44px] (inherited from Button component). |

**Source:** Phase 1 UI-SPEC accessibility contract, extended for FAQ accordion pattern. WCAG 2.1 AA accordion pattern guidance.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable -- shadcn not initialized |
| Third-party | none | not applicable |

**Note:** Phase 2 introduces zero new dependencies and no component registry blocks. All new components (CityIntro, NeighborhoodGrid, CityFAQ) are hand-built with Tailwind CSS v4 utility classes, consistent with Phase 1 approach.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
