---
phase: 01-foundation-infrastructure-homepage
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - app/layout.tsx
  - components/layout/Header.tsx
  - components/layout/Footer.tsx
  - components/layout/Navigation.tsx
  - components/layout/MobileNav.tsx
autonomous: true
requirements:
  - FNDN-05
  - FNDN-06
  - LEAD-01
  - SEO-06
  - SILO-05

must_haves:
  truths:
    - "Every page shows a sticky navy header with logo, navigation links, and amber phone CTA button"
    - "Desktop navigation has Services and Locations dropdown menus"
    - "Mobile hamburger menu opens a full-screen overlay with all nav items"
    - "Phone number is clickable on all viewports and visible in the header at all times"
    - "Footer displays 4 columns of content with business info, service links, location links, and contact details"
    - "All internal links use next/link component"
  artifacts:
    - path: "components/layout/Header.tsx"
      provides: "Sticky header with nav and phone CTA"
      contains: "sticky top-0"
    - path: "components/layout/Footer.tsx"
      provides: "4-column footer with bottom bar"
      contains: "siteConfig.businessName"
    - path: "components/layout/Navigation.tsx"
      provides: "Desktop nav with dropdown menus"
      contains: "navigationItems"
    - path: "components/layout/MobileNav.tsx"
      provides: "Mobile hamburger menu overlay"
      contains: "'use client'"
    - path: "app/layout.tsx"
      provides: "Root layout wrapping children with Header + Footer"
      contains: "Header"
  key_links:
    - from: "app/layout.tsx"
      to: "components/layout/Header.tsx"
      via: "import and render"
      pattern: "import.*Header"
    - from: "app/layout.tsx"
      to: "components/layout/Footer.tsx"
      via: "import and render"
      pattern: "import.*Footer"
    - from: "components/layout/Header.tsx"
      to: "components/ui/PhoneButton.tsx"
      via: "import and render"
      pattern: "PhoneButton"
    - from: "components/layout/Header.tsx"
      to: "components/layout/Navigation.tsx"
      via: "import and render"
      pattern: "Navigation"
    - from: "components/layout/Header.tsx"
      to: "components/layout/MobileNav.tsx"
      via: "import and render"
      pattern: "MobileNav"
    - from: "components/layout/Footer.tsx"
      to: "lib/site-config.ts"
      via: "import siteConfig"
      pattern: "siteConfig"
---

<objective>
Build the complete layout shell -- sticky header with navigation dropdowns and phone CTA, mobile hamburger menu, and 4-column footer -- then wire into the root layout so every page gets the shell.

Purpose: Establishes the persistent page frame (FNDN-06) that wraps all content. The header provides primary navigation and the always-visible phone CTA (LEAD-01). The footer provides secondary navigation and business information. This layout is inherited by every page in subsequent phases.
Output: Header.tsx, Footer.tsx, Navigation.tsx, MobileNav.tsx components, and updated layout.tsx with shell wrapper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-infrastructure-homepage/01-CONTEXT.md
@.planning/phases/01-foundation-infrastructure-homepage/01-RESEARCH.md
@.planning/phases/01-foundation-infrastructure-homepage/01-UI-SPEC.md
@.planning/phases/01-foundation-infrastructure-homepage/01-02-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 02 that this plan depends on -->

From lib/cn.ts:
```typescript
export function cn(...inputs: ClassValue[]): string
```

From lib/site-config.ts:
```typescript
export const siteConfig: {
  businessName: string
  phone: string
  phoneRaw: string
  email: string
  url: string
  businessHours: Array<{ day: string; opens: string; closes: string }>
  stats: { ... }
  municipalities: Array<{ name: string; slug: string; type: string; cluster: string; population: number }>
}
```

From lib/navigation.ts:
```typescript
export const navigationItems: Array<{
  label: string
  href: string
  children?: Array<{ label: string; href: string }>
}>
```

From components/ui/PhoneButton.tsx:
```typescript
export function PhoneButton({ compact, className }: { compact?: boolean; className?: string }): JSX.Element
```

From components/ui/Button.tsx:
```typescript
export function Button({ variant, size, children, className, ...props }): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Header with desktop navigation, dropdowns, and mobile hamburger trigger</name>
  <files>components/layout/Header.tsx, components/layout/Navigation.tsx, components/layout/MobileNav.tsx</files>
  <read_first>
    - components/ui/PhoneButton.tsx (import path and props for phone CTA in header)
    - components/ui/Button.tsx (import path and props for any CTA buttons)
    - lib/navigation.ts (navigationItems structure for menu rendering)
    - lib/site-config.ts (businessName for logo text)
    - lib/cn.ts (import path for cn utility)
    - .planning/phases/01-foundation-infrastructure-homepage/01-UI-SPEC.md (Interaction Contracts: Sticky Header, Navigation Dropdowns, Accessibility Contract)
    - .planning/phases/01-foundation-infrastructure-homepage/01-CONTEXT.md (D-05 header layout, D-15 phone CTA)
  </read_first>
  <action>
**1. Create `components/layout/Navigation.tsx` (Server Component):**

Desktop navigation bar showing main nav items from `navigationItems`. Per D-05: Services dropdown, Locations dropdown, Guides, About, Contact centered in the header.

- Import `navigationItems` from `@/lib/navigation`
- Import `siteConfig` from `@/lib/site-config` (for populating Locations dropdown children from municipalities)
- Import `Link` from `next/link` (per SEO-06)
- Import `cn` from `@/lib/cn`

Render a `<nav>` element with `<ul>` of nav items:
- Items WITHOUT children: Simple `<Link>` with text-white font-semibold hover:text-amber-light transition-colors
- Items WITH children (Services, Locations): Render as a `<div>` with the label as trigger and a dropdown panel

Dropdown behavior (CSS-only for server component, per UI-SPEC Interaction Contracts):
- Use `group` and `group-hover:` Tailwind utilities for hover-to-open on desktop
- Dropdown panel: `absolute top-full left-0 mt-1 w-[280px] bg-white shadow-lg rounded-lg z-50 py-2 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200`
- Dropdown items: `block px-4 py-2 text-lg text-navy hover:bg-amber-light/10 transition-colors` using `<Link>` for each child item
- For Locations dropdown: dynamically build children from `siteConfig.municipalities.map(m => ({ label: m.name, href: `/roofing-contractor-${m.slug}-nj` }))`

Desktop only: `hidden md:flex` -- mobile uses MobileNav instead.

Each nav link must have appropriate min-h-[44px] and padding for touch target compliance.

**2. Create `components/layout/MobileNav.tsx` (Client Component -- `'use client'`):**

Full-screen mobile menu overlay. Per UI-SPEC: slide-in from right, navy background, z-40, close on backdrop click or X button.

Props: `isOpen: boolean`, `onClose: () => void`

- Import `useState` from 'react' (for accordion expand/collapse of dropdown sections)
- Import `navigationItems` from `@/lib/navigation`
- Import `siteConfig` from `@/lib/site-config`
- Import `Link` from `next/link`
- Import `X`, `ChevronDown` from `lucide-react`
- Import `PhoneButton` from `@/components/ui/PhoneButton`
- Import `cn` from `@/lib/cn`

Structure:
```
<div> (overlay backdrop -- fixed inset-0 bg-black/50 z-40, onClick={onClose}, hidden when !isOpen)
  <div> (slide panel -- fixed right-0 top-0 h-full w-[300px] max-w-[85vw] bg-navy z-50, transform translateX transition)
    <div> (header -- flex justify-between items-center p-4)
      <span>Menu</span> (text-white font-heading text-xl)
      <button onClick={onClose} aria-label="Close navigation menu">
        <X className="h-6 w-6 text-white" />
      </button>
    </div>
    <nav> (flex-1 overflow-y-auto)
      <ul>
        {navigationItems.map(item => (
          // Items without children: simple Link
          // Items with children: accordion pattern
          //   Label with ChevronDown icon, onClick toggles expanded state
          //   Expanded: show child links indented
          //   For Locations: build children from siteConfig.municipalities
        ))}
      </ul>
    </nav>
    <div> (bottom area -- p-4)
      <PhoneButton compact={false} className="w-full justify-center" />
    </div>
  </div>
</div>
```

All links: text-white text-lg py-3 px-4 min-h-[44px] border-b border-navy-light, hover:bg-navy-light transition-colors.

Close menu when a link is clicked (call onClose in onClick handler alongside navigation).

Accessibility:
- Close button: `aria-label="Close navigation menu"`
- Escape key closes menu: add `useEffect` with keydown listener for 'Escape' key
- Trap focus within the panel when open (optional enhancement)
- `motion-reduce:` prefix on slide transition

**3. Create `components/layout/Header.tsx` (Server Component wrapping Client MobileNav):**

Per D-05: Logo left, main nav center, phone CTA button right. Hamburger on mobile.

- Import `Navigation` from `@/components/layout/Navigation`
- Import `PhoneButton` from `@/components/ui/PhoneButton`
- Import `Link` from `next/link`
- Import `cn` from `@/lib/cn`
- Import `siteConfig` from `@/lib/site-config`

Since Header is a Server Component but needs the hamburger menu toggle state, create an inner client component `HeaderClient` (either inline or in the same file with 'use client' boundary):

Actually, use a pattern where Header.tsx is a Server Component that renders the static parts and imports a `MobileMenuButton` client component for just the hamburger toggle + MobileNav.

Create a small client wrapper inside Header.tsx or as a separate tiny component:

```typescript
// Inside components/layout/Header.tsx
// The header itself is a server component for the static parts

// At the top of the file or as a separate import:
// components/layout/MobileMenuButton.tsx ('use client')
```

Better approach: Make the entire Header a client component since the mobile menu state needs to be managed. The Nav dropdowns can still work with CSS-only hover, but the hamburger toggle needs useState. Mark Header.tsx with `'use client'`.

Structure:
```html
<header class="sticky top-0 z-50 bg-navy backdrop-blur-sm">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-16">
      <!-- Logo (left) -->
      <Link href="/" class="flex items-center">
        <span class="text-white font-heading text-xl font-bold">
          {siteConfig.businessName}
        </span>
      </Link>

      <!-- Desktop Navigation (center) -->
      <Navigation className="hidden md:flex" />

      <!-- Desktop Phone CTA (right) -->
      <PhoneButton compact={false} className="hidden md:inline-flex" />

      <!-- Mobile: Phone + Hamburger -->
      <div class="flex items-center gap-2 md:hidden">
        <PhoneButton compact={true} />
        <button onClick={toggleMenu} aria-label="Open navigation menu" aria-expanded={isMenuOpen}>
          <Menu className="h-6 w-6 text-white" />
        </button>
      </div>
    </div>
  </div>
</header>

<!-- Skip to main content link (first focusable element, per Accessibility Contract) -->
<!-- Place BEFORE the header: -->
<a href="#main-content" class="sr-only focus:not-sr-only focus:absolute focus:z-[60] focus:bg-amber focus:text-white focus:px-4 focus:py-2 focus:rounded">
  Skip to main content
</a>

<MobileNav isOpen={isMenuOpen} onClose={() => setIsMenuOpen(false)} />
```

The "Skip to main content" link must be the FIRST focusable element on the page. Place it before the header element within the Header component's return, or better yet, place it in the root layout before the Header.

Import `Menu` icon from `lucide-react` for hamburger.

Mark Header.tsx with `'use client'` since it manages isMenuOpen state.
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/patersonroofingcontractors && npx tsc --noEmit 2>&1 | head -20 && echo "---" && ls components/layout/Header.tsx components/layout/Navigation.tsx components/layout/MobileNav.tsx 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - components/layout/Header.tsx exists and contains "sticky top-0 z-50"
    - components/layout/Header.tsx contains "bg-navy"
    - components/layout/Header.tsx imports PhoneButton from @/components/ui/PhoneButton
    - components/layout/Header.tsx imports Navigation
    - components/layout/Header.tsx contains "aria-label" with "Open navigation menu"
    - components/layout/Header.tsx contains "aria-expanded" for hamburger button
    - components/layout/Header.tsx contains "Skip to main content" link
    - components/layout/Navigation.tsx imports navigationItems from @/lib/navigation
    - components/layout/Navigation.tsx imports Link from next/link
    - components/layout/Navigation.tsx contains dropdown panel with "group-hover:" or equivalent hover trigger
    - components/layout/Navigation.tsx contains "z-50" on dropdown panels
    - components/layout/MobileNav.tsx contains "'use client'" directive
    - components/layout/MobileNav.tsx contains "aria-label" with "Close navigation menu"
    - components/layout/MobileNav.tsx contains Escape key handler (addEventListener.*keydown.*Escape)
    - components/layout/MobileNav.tsx contains PhoneButton import
    - components/layout/MobileNav.tsx contains "motion-reduce:" on transition classes
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Header, Navigation, and MobileNav components created. Header is sticky with navy background, desktop nav with hover dropdowns, phone CTA always visible, hamburger menu on mobile opening a slide-in overlay.</done>
</task>

<task type="auto">
  <name>Task 2: Footer and layout.tsx shell integration</name>
  <files>components/layout/Footer.tsx, app/layout.tsx</files>
  <read_first>
    - app/layout.tsx (current state from Plan 02 -- has fonts, metadata, analytics; needs Header/Footer wrapper)
    - components/layout/Header.tsx (just created -- import path)
    - lib/site-config.ts (businessName, phone, email, businessHours, municipalities for footer content)
    - lib/navigation.ts (service links for footer quick links column)
    - data/services.ts (service names for footer column 2)
    - .planning/phases/01-foundation-infrastructure-homepage/01-UI-SPEC.md (Homepage Section Order item 12: Footer specs)
    - .planning/phases/01-foundation-infrastructure-homepage/01-CONTEXT.md (D-06 footer layout spec)
  </read_first>
  <action>
**1. Create `components/layout/Footer.tsx` (Server Component):**

Per D-06: Full 4-column footer with navy background and navy-dark bottom bar.

- Import `Link` from `next/link` (per SEO-06)
- Import `siteConfig` from `@/lib/site-config`
- Import `services` from `@/data/services`
- Import `Phone`, `Mail`, `MapPin`, `Clock` from `lucide-react`
- Import `cn` from `@/lib/cn`

Structure:
```html
<footer class="bg-navy text-white">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-section-sm lg:py-section">
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-xl">

      <!-- Column 1: Logo + Business Description + Phone -->
      <div>
        <Link href="/" class="font-heading text-2xl font-bold text-white">
          {siteConfig.businessName}
        </Link>
        <p class="mt-4 text-gray-300 text-lg">
          Professional roofing services for Passaic County, NJ. Licensed, insured, and committed to quality craftsmanship for over 15 years.
        </p>
        <a href={`tel:${siteConfig.phoneRaw}`} class="mt-4 inline-flex items-center gap-2 text-amber-light hover:text-amber font-semibold transition-colors">
          <Phone class="h-5 w-5" />
          {siteConfig.phone}
        </a>
      </div>

      <!-- Column 2: Services Quick Links -->
      <div>
        <h3 class="font-heading text-xl font-bold text-white mb-4">Our Services</h3>
        <ul class="space-y-2">
          {services.slice(0, 8).map(service => (
            <li>
              <Link href={`/services/${service.slug}`} class="text-gray-300 hover:text-amber-light transition-colors">
                {service.name}
              </Link>
            </li>
          ))}
        </ul>
      </div>

      <!-- Column 3: Service Area Municipality Links -->
      <div>
        <h3 class="font-heading text-xl font-bold text-white mb-4">Service Areas</h3>
        <ul class="space-y-2">
          {siteConfig.municipalities.map(m => (
            <li>
              <Link href={`/roofing-contractor-${m.slug}-nj`} class="text-gray-300 hover:text-amber-light transition-colors">
                {m.name}
              </Link>
            </li>
          ))}
        </ul>
      </div>

      <!-- Column 4: Contact Info + Business Hours -->
      <div>
        <h3 class="font-heading text-xl font-bold text-white mb-4">Contact Us</h3>
        <ul class="space-y-3">
          <li class="flex items-start gap-2">
            <Phone class="h-5 w-5 text-amber-light mt-0.5 shrink-0" />
            <a href={`tel:${siteConfig.phoneRaw}`} class="text-gray-300 hover:text-amber-light">{siteConfig.phone}</a>
          </li>
          <li class="flex items-start gap-2">
            <Mail class="h-5 w-5 text-amber-light mt-0.5 shrink-0" />
            <a href={`mailto:${siteConfig.email}`} class="text-gray-300 hover:text-amber-light">{siteConfig.email}</a>
          </li>
          <li class="flex items-start gap-2">
            <MapPin class="h-5 w-5 text-amber-light mt-0.5 shrink-0" />
            <span class="text-gray-300">Passaic County, NJ</span>
          </li>
          <li class="flex items-start gap-2">
            <Clock class="h-5 w-5 text-amber-light mt-0.5 shrink-0" />
            <div class="text-gray-300">
              <p>Mon-Fri: 7:00 AM - 6:00 PM</p>
              <p>Sat: 8:00 AM - 2:00 PM</p>
              <p>Sun: Closed</p>
            </div>
          </li>
        </ul>
      </div>

    </div>
  </div>

  <!-- Bottom Bar -->
  <div class="bg-navy-dark">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
      <div class="flex flex-col sm:flex-row justify-between items-center gap-2 text-sm text-gray-400">
        <p>&copy; {new Date().getFullYear()} {siteConfig.businessName}. All rights reserved.</p>
        <div class="flex gap-4">
          <Link href="/privacy-policy" class="hover:text-amber-light transition-colors">Privacy Policy</Link>
          <Link href="/terms-of-service" class="hover:text-amber-light transition-colors">Terms of Service</Link>
        </div>
      </div>
    </div>
  </div>
</footer>
```

All links use `next/link` (per SEO-06). Footer body text is gray-300 (lighter than white for hierarchy). Heading font used for section titles. Icons from lucide-react in amber-light color.

**2. Update `app/layout.tsx` to wrap children with Header and Footer:**

Read the current layout.tsx (from Plan 02). Add imports for Header and Footer:
```typescript
import { Header } from '@/components/layout/Header'
import { Footer } from '@/components/layout/Footer'
```

Update the body content to:
```tsx
<body>
  <Header />
  <main id="main-content">
    {children}
  </main>
  <Footer />
  <Analytics />
  <SpeedInsights />
</body>
```

The `id="main-content"` on `<main>` is the target for the "Skip to main content" link in Header.tsx (per Accessibility Contract).

Do NOT remove any existing metadata exports, font configurations, or analytics imports. Only modify the body JSX to add the layout shell wrapper.
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/patersonroofingcontractors && npx tsc --noEmit 2>&1 | head -20 && echo "---" && grep -c "next/link" components/layout/Footer.tsx</automated>
  </verify>
  <acceptance_criteria>
    - components/layout/Footer.tsx exists and contains "bg-navy" for main footer background
    - components/layout/Footer.tsx contains "bg-navy-dark" for bottom bar
    - components/layout/Footer.tsx contains siteConfig.businessName reference
    - components/layout/Footer.tsx contains siteConfig.phone reference
    - components/layout/Footer.tsx contains siteConfig.municipalities mapping for service area links
    - components/layout/Footer.tsx contains at least 10 Link imports/usages from next/link (services + municipalities + privacy + terms)
    - components/layout/Footer.tsx contains "Privacy Policy" link to /privacy-policy
    - components/layout/Footer.tsx contains "Terms of Service" link to /terms-of-service
    - components/layout/Footer.tsx contains heading "Our Services"
    - components/layout/Footer.tsx contains heading "Service Areas"
    - components/layout/Footer.tsx contains heading "Contact Us"
    - app/layout.tsx imports Header from @/components/layout/Header
    - app/layout.tsx imports Footer from @/components/layout/Footer
    - app/layout.tsx contains '<main id="main-content">'
    - app/layout.tsx still contains Cormorant_Garamond font configuration (not removed)
    - app/layout.tsx still contains metadata export with title template (not removed)
    - app/layout.tsx still contains Analytics and SpeedInsights
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Footer with 4-column layout and navy-dark bottom bar created. Root layout.tsx updated to wrap children with Header + main + Footer. Every page now inherits the sticky header, navigation, phone CTA, and footer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External link targets | Footer links to /privacy-policy, /terms-of-service, service pages, location pages -- all internal, no external URLs |
| Tel: links | Phone number click-to-call via tel: protocol -- user-initiated action only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Spoofing | Navigation links | accept | All links are internal (next/link to app routes). No external URLs that could be spoofed. |
| T-01-03-02 | Denial of Service | MobileNav state management | accept | Simple boolean state toggle. No complex state, no API calls, no resources to exhaust. |
| T-01-03-03 | Cross-Site Scripting | Header/Footer rendering | accept | All content comes from static imports (site-config.ts, navigation.ts). No user input rendered. All text is hardcoded or from typed constants. |
</threat_model>

<verification>
- TypeScript compiles without errors: `npx tsc --noEmit`
- Dev server starts and renders: `npm run dev` shows header, navigation, and footer
- All navigation links use next/link (no raw `<a>` tags with full URLs)
- Phone CTA visible on both desktop and mobile viewports
- Mobile hamburger menu opens and closes
- Skip-to-main-content link exists and targets #main-content
</verification>

<success_criteria>
- Sticky header with logo, desktop nav (with hover dropdowns for Services/Locations), and phone CTA
- Mobile: hamburger menu with slide-in overlay, phone CTA always visible
- Desktop: CSS-only hover dropdowns showing service links and all 16 municipality links
- Footer: 4-column layout with business info, service links, municipality links, contact details
- Bottom bar: copyright + privacy/terms links
- Root layout wraps all pages with Header/main/Footer
- All interactive elements meet 44px touch target minimum
- Keyboard accessible (Escape closes mobile menu, focus-visible states, skip link)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-infrastructure-homepage/01-03-SUMMARY.md`
</output>
