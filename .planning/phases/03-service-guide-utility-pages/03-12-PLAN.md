---
phase: 03-service-guide-utility-pages
plan: 12
type: execute
wave: 4
depends_on: [03-07, 03-08, 03-09, 03-10, 03-11]
files_modified:
  - app/sitemap.ts
autonomous: false
requirements: [SVC-01, GUIDE-01, UTIL-01, UTIL-02, UTIL-03, SILO-02, SILO-03]

must_haves:
  truths:
    - "Sitemap includes all 67 service pages with priority 0.7"
    - "Sitemap includes all 10 guide pages with priority 0.6"
    - "Sitemap includes service index, guide index, about, contact, service-area pages"
    - "Sitemap excludes privacy-policy and terms-of-service (noindex pages)"
    - "Total sitemap entries: ~100 (1 homepage + 16 locations + 67 services + 10 guides + 2 index + 3 utility)"
  artifacts:
    - path: "app/sitemap.ts"
      provides: "Complete XML sitemap covering all ~100 pages"
      contains: "services"
  key_links:
    - from: "app/sitemap.ts"
      to: "data/services.ts"
      via: "services import for service page URLs"
      pattern: "import.*services.*from.*data/services"
    - from: "app/sitemap.ts"
      to: "data/guides/content/index.ts"
      via: "getAllGuideSlugs for guide page URLs"
      pattern: "getAllGuideSlugs"
---

<objective>
Update the XML sitemap to include all Phase 3 pages (67 services, 10 guides, 5 utility pages minus 2 noindex), then verify the full site builds and renders correctly.

Purpose: Completes the sitemap covering all ~100 pages for search engine crawling. The verification checkpoint confirms the entire Phase 3 delivery -- every page type renders, navigation works, schema markup present, and metadata correct.

Output: Updated sitemap.ts + human verification of full site
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-service-guide-utility-pages/03-CONTEXT.md
@.planning/phases/03-service-guide-utility-pages/03-RESEARCH.md

<interfaces>
From app/sitemap.ts (current state):
```typescript
import type { MetadataRoute } from 'next'
import { siteConfig } from '@/lib/site-config'

export default function sitemap(): MetadataRoute.Sitemap {
  const baseUrl = 'https://patersonroofingcontractors.com'
  const locationPages = siteConfig.municipalities.map((m) => ({
    url: `${baseUrl}/roofing-contractor-${m.slug}-nj`,
    lastModified: new Date(),
    changeFrequency: 'monthly' as const,
    priority: /* 0.8 or 0.9 for paterson */
  }))
  // homepage + locationPages
}
```

From data/services.ts:
```typescript
export const services: readonly Service[] = [ /* 67 entries */ ]
```

From data/guides/content/index.ts:
```typescript
export function getAllGuideSlugs(): string[]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update sitemap with all Phase 3 pages</name>
  <files>app/sitemap.ts</files>
  <read_first>
    - app/sitemap.ts (current implementation with homepage + 16 location pages)
    - data/services.ts (services array -- import for service page URLs)
    - data/guides/content/index.ts (getAllGuideSlugs function for guide URLs)
    - .planning/phases/03-service-guide-utility-pages/03-RESEARCH.md (sitemap extension code example with priorities)
  </read_first>
  <action>
Update `app/sitemap.ts` to include all Phase 3 pages. Keep existing homepage and location page entries. Add:

```typescript
import { services } from '@/data/services'
import { getAllGuideSlugs } from '@/data/guides/content'
```

Add service pages (67 entries):
```typescript
const servicePages = services.map((s) => ({
  url: `${baseUrl}/services/${s.slug}`,
  lastModified: new Date(),
  changeFrequency: 'monthly' as const,
  priority: 0.7,
}))
```

Add guide pages (10 entries):
```typescript
const guidePages = getAllGuideSlugs().map((slug) => ({
  url: `${baseUrl}/roofing-guides/${slug}`,
  lastModified: new Date(),
  changeFrequency: 'monthly' as const,
  priority: 0.6,
}))
```

Add index and utility pages (5 entries, excluding privacy/terms per CRAWL-05):
```typescript
const utilityPages = [
  { url: `${baseUrl}/services`, priority: 0.8 },
  { url: `${baseUrl}/roofing-guides`, priority: 0.7 },
  { url: `${baseUrl}/about`, priority: 0.5 },
  { url: `${baseUrl}/contact`, priority: 0.6 },
  { url: `${baseUrl}/service-area`, priority: 0.6 },
].map((p) => ({
  ...p,
  lastModified: new Date(),
  changeFrequency: 'monthly' as const,
}))
```

Return all combined: `return [homepage, ...locationPages, ...servicePages, ...guidePages, ...utilityPages]`

Do NOT include /privacy-policy or /terms-of-service in sitemap (they have noindex per D-25 and CRAWL-05 requires no noindex pages in sitemap).

Expected total: 1 (homepage) + 16 (locations) + 67 (services) + 10 (guides) + 5 (utility/index) = 99 entries.
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/patersonroofingcontractors && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <acceptance_criteria>
    - app/sitemap.ts contains `import { services }`
    - app/sitemap.ts contains `import { getAllGuideSlugs }`
    - app/sitemap.ts contains `/services/${` (service URL pattern)
    - app/sitemap.ts contains `/roofing-guides/` (guide URL pattern)
    - app/sitemap.ts contains `'/about'` or `/about`
    - app/sitemap.ts contains `'/contact'` or `/contact`
    - app/sitemap.ts contains `'/service-area'` or `/service-area`
    - app/sitemap.ts does NOT contain `privacy-policy`
    - app/sitemap.ts does NOT contain `terms-of-service`
    - TypeScript compiles with zero errors
  </acceptance_criteria>
  <done>Sitemap updated with ~99 entries covering all public pages. Privacy/Terms excluded. TypeScript compiles cleanly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 3 delivery</name>
  <action>
Human verifies the full Phase 3 delivery: 67 service pages, 10 guide pages, 5 utility pages, 2 index pages, mega menu navigation, Service JSON-LD schema, and updated sitemap.
  </action>
  <what-built>
Complete Phase 3 delivery: 67 service pages, 10 guide pages, 5 utility pages, 2 index pages, mega menu navigation, Service JSON-LD schema, and updated sitemap.
  </what-built>
  <how-to-verify>
1. Start the dev server: `npm run dev`
2. Visit http://localhost:3000 -- homepage should still render correctly
3. Open the Services mega menu in navigation -- should show categories with services grouped
4. Visit http://localhost:3000/services -- should show all services grouped by category
5. Visit http://localhost:3000/services/roof-repair -- should show full service page with all sections (Overview, Process Steps, Benefits, Related Locations, Related Services, FAQ, Testimonials, Emergency CTA, Contact Form)
6. Check JSON-LD: View page source on a service page, search for "application/ld+json" -- should find Service and FAQ schemas
7. Visit http://localhost:3000/roofing-guides -- should show guide cards with read time
8. Visit http://localhost:3000/roofing-guides/complete-nj-roofing-guide-homeowners -- should show article with sidebar TOC on desktop, collapsible TOC on mobile. Scroll to verify TOC active indicator updates
9. Visit http://localhost:3000/about -- company story, credentials, WhyChooseUs
10. Visit http://localhost:3000/contact -- ContactHub with form + business info panel
11. Visit http://localhost:3000/service-area -- municipality grid with population and cluster tags
12. Visit http://localhost:3000/privacy-policy -- legal text, no hero, no CTA
13. Visit http://localhost:3000/terms-of-service -- legal text, no hero, no CTA
14. Check mobile responsive: resize browser to mobile width, verify mega menu becomes accordion, TOC collapses
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sitemap generation | URLs generated from static data arrays |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-19 | I (Information Disclosure) | Sitemap URL enumeration | accept | All URLs are intentionally public. Site is designed for search engine discovery. No private pages exist. |
</threat_model>

<verification>
- `npx tsc --noEmit` passes
- `npm run build` completes successfully generating ~100 static pages
- Sitemap has ~99 entries
- All page types render correctly
</verification>

<success_criteria>
- Sitemap covers all ~99 public pages (excluding noindex Privacy/Terms)
- Full site builds without errors
- All page types visually verified: service, guide, utility, index pages
- Mega menu navigation works on desktop and mobile
- Service JSON-LD and FAQ JSON-LD present on service pages
- Guide sidebar TOC tracks scroll position correctly
</success_criteria>

<output>
After completion, create `.planning/phases/03-service-guide-utility-pages/03-12-SUMMARY.md`
</output>
